---js
{
  layout: 'layouts/base.njk',
  permalink: '/{{ locale }}/index.html',
  pluralize: function(values, count) {
    var cases = [2, 0, 1, 1, 1, 2];
    return values[
      count % 100 > 4 && count % 100 < 20 ? 2 : cases[Math.min(count % 10, 5)]
    ];
  }
}
---